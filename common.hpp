#include <bits/stdc++.h>

using namespace std;

class Picture {
public:
  vector<string> tags;
};

vector<Picture> input;

class Slide {
public:
  int first;
  int second;
};

class Slideshow {
public:
  vector<Slide> show;

  unsigned long long evaluate() {
    
  }
}
